# todo

'Per frame override' next to ones that have been changed should have a reset option/button
same for the frame settings if it doesn't already have it

and then make the frame reorganisation better
drag and dropping frames to reorganise is really bad there's no indication of where its gonna end up
it should give like a highliighted vertical line inbetween the two frames to show like it'll go between these two frames


idk if you can already... but you should also be able to multi select and do options on that

so like 'Frame settings' will show 'mulitple selected' or whatever and let you do stuff
obvs probs not like move it about within the canvas cos that makes no sense
but like change the fit override
andddd
also like yeah when multiple are selected... move them alllll in the time line forward / back

ALSO
should be able to like then duplicate multiple frames at once when multiple selected or whatever
and delete multiple when they're selected etc


also.... be able to save your progress..???
idk how it would save the images...
but like use json or whatever for global settings  and frame settings
